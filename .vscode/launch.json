{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch Package",
            "type": "go",
            "request": "launch",
            "mode": "auto",
            "program": "${workspaceFolder}",
            "args": ["--ll=debug", "--dump=dump"],
            "cwd": "${workspaceFolder}/examples/01 golang-webservice+postgresql"
        }
    ]
}